# Build output
native/CUE.app/
native/CUE
native/*.zip
native/*.o

# Local, machine-specific build config (paths, signing identity)
native/cue.conf

# Deck backups (written into the decks folder, but ignore if ever present here)
.cue-backups/
.backups/

# macOS
.DS_Store

# Node (none used, but just in case)
node_modules/
npm-debug.log*

# Build output
dist/CUE-portable.zip
native/CUE.app/
brand/AppIcon.iconset/
